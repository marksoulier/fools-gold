"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.observingComponents = observingComponents;
const core_1 = require("@babel/core");
const babel_plugin_observing_components_1 = __importDefault(require("babel-plugin-observing-components"));
function observingComponents(options) {
    return {
        name: "vite-plugin-observing-components",
        enforce: "pre",
        async transform(code, id) {
            var _a;
            // Only process JS/TS/JSX/TSX files
            if (!/\.[jt]sx?$/.test(id))
                return null;
            // Skip node_modules
            if (id.includes("node_modules"))
                return null;
            const result = await (0, core_1.transformAsync)(code, {
                filename: id,
                plugins: [(0, babel_plugin_observing_components_1.default)(options)],
                sourceMaps: true,
                // Don't read any project babel config — only our plugin runs here
                configFile: false,
                babelrc: false,
                parserOpts: {
                    plugins: ["jsx", "typescript"],
                },
            });
            if (!result || result.code == null)
                return null;
            return {
                code: result.code,
                map: (_a = result.map) !== null && _a !== void 0 ? _a : null,
            };
        },
    };
}
//# sourceMappingURL=index.js.map