"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transform = void 0;
exports.default = createPlugin;
const minimatch_1 = require("minimatch");
// Explicit imports to avoid tslib dependency
const types_1 = require("@babel/types");
// Helper function to check if identifier starts with uppercase letter
function startsWithUppercase(name) {
    return /^[A-Z]/.test(name);
}
// Helper function to check if a function is a component (has JSX and starts with uppercase)
function isComponentFunction(path) {
    // Check for JSX
    let hasJsx = false;
    path.traverse({
        JSX() {
            hasJsx = true;
            // Don't call path.stop() here as it stops traversal for the entire parent path
        },
    });
    if (!hasJsx)
        return false;
    // Check if the identifier starts with uppercase
    let functionName = null;
    if ((0, types_1.isFunctionDeclaration)(path.node) && path.node.id) {
        // Standalone function declaration
        functionName = path.node.id.name;
    }
    else {
        // For function expressions, we need to look up further in the AST
        let currentPath = path;
        let found = false;
        // Look up through ancestors to find variable declarator, assignment, or object property
        while (currentPath.parentPath && !found) {
            currentPath = currentPath.parentPath;
            // Direct variable declaration: const Component = () => {}
            if ((0, types_1.isVariableDeclarator)(currentPath.node) &&
                (0, types_1.isIdentifier)(currentPath.node.id)) {
                functionName = currentPath.node.id.name;
                found = true;
            }
            // Skip object properties - we'll handle these separately
            else if ((0, types_1.isObjectProperty)(currentPath.node)) {
                // Don't process object properties here
                return false;
            }
            // Handle function wrapped in another function: const Component = foo(() => {})
            else if ((0, types_1.isCallExpression)(currentPath.node)) {
                // Keep looking up
                continue;
            }
            // If we reach a statement level without finding an identifier, stop looking
            else if (currentPath.isStatement()) {
                break;
            }
        }
    }
    return functionName !== null && startsWithUppercase(functionName);
}
exports.transform = {
    name: "wrap-with-observer",
    visitor: {
        Program(path, state) {
            // @ts-ignore
            const filename = state.filename || state.opts.filename;
            // @ts-ignore
            const IMPORT_PATH = state.opts.importPath;
            // @ts-ignore
            const IMPORT_NAME = state.opts.importName;
            // @ts-ignore
            const EXCLUDE_PATTERNS = state.opts.exclude;
            if (filename && !shouldProcessFile(filename, EXCLUDE_PATTERNS)) {
                return;
            }
            let hasObserverImport = false;
            let transformedJSX = false; // renamed flag
            path.traverse({
                ImportDeclaration(importPath) {
                    if (importPath.node.source.value === IMPORT_PATH) {
                        const hasObserverSpecifier = importPath.node.specifiers.some((specifier) => (0, types_1.isImportSpecifier)(specifier) &&
                            (0, types_1.isIdentifier)(specifier.imported) &&
                            specifier.imported.name === IMPORT_NAME);
                        if (!hasObserverSpecifier) {
                            importPath.node.specifiers.push((0, types_1.importSpecifier)((0, types_1.identifier)(IMPORT_NAME), (0, types_1.identifier)(IMPORT_NAME)));
                        }
                        hasObserverImport = true;
                    }
                },
            });
            // Wrap any function returning JSX with observer
            path.traverse({
                FunctionDeclaration(functionPath) {
                    if (isComponentFunction(functionPath)) {
                        const hasWrapped = wrapFunctionWithObserver(functionPath, IMPORT_NAME);
                        transformedJSX = transformedJSX || hasWrapped;
                        functionPath.skip();
                    }
                },
                FunctionExpression(functionPath) {
                    // Skip if parent is ObjectProperty - we don't want to transform these
                    if ((0, types_1.isObjectProperty)(functionPath.parent)) {
                        return;
                    }
                    if (isComponentFunction(functionPath)) {
                        const hasWrapped = wrapFunctionWithObserver(functionPath, IMPORT_NAME);
                        transformedJSX = transformedJSX || hasWrapped;
                        functionPath.skip();
                    }
                },
                ArrowFunctionExpression(functionPath) {
                    // Skip if parent is ObjectProperty - we don't want to transform these
                    if ((0, types_1.isObjectProperty)(functionPath.parent)) {
                        return;
                    }
                    if (isComponentFunction(functionPath)) {
                        const hasWrapped = wrapFunctionWithObserver(functionPath, IMPORT_NAME);
                        transformedJSX = transformedJSX || hasWrapped;
                        functionPath.skip();
                    }
                },
            });
            // Only add import if functions with JSX were transformed
            if (!hasObserverImport && transformedJSX) {
                const _importDeclaration = (0, types_1.importDeclaration)([(0, types_1.importSpecifier)((0, types_1.identifier)(IMPORT_NAME), (0, types_1.identifier)(IMPORT_NAME))], (0, types_1.stringLiteral)(IMPORT_PATH));
                path.unshiftContainer("body", _importDeclaration);
            }
        },
    },
};
function shouldProcessFile(filename, excludePatterns) {
    const isNodeModule = filename.includes("node_modules");
    const isProjectFile = filename.startsWith(process.cwd());
    // Skip files that match any exclude pattern
    if (excludePatterns && excludePatterns.length > 0) {
        // Convert absolute path to relative path for matching
        const relativePath = filename.substring(process.cwd().length + 1);
        for (const pattern of excludePatterns) {
            if ((0, minimatch_1.minimatch)(relativePath, pattern)) {
                return false;
            }
        }
    }
    return !isNodeModule && isProjectFile;
}
function wrapFunctionWithObserver(functionPath, observerName) {
    const funcNode = functionPath.node;
    const parentNode = functionPath.parent;
    const parentPath = functionPath.parentPath;
    // Check if the function is already wrapped with observer
    if ((0, types_1.isCallExpression)(parentNode) &&
        (0, types_1.isIdentifier)(parentNode.callee) &&
        parentNode.callee.name === observerName) {
        // If we're in a variable declaration, leave it as is
        if ((parentPath === null || parentPath === void 0 ? void 0 : parentPath.parent) && (0, types_1.isVariableDeclarator)(parentPath.parent)) {
            return false;
        }
        // Otherwise, wrap the entire observer call
        const observerCall = (0, types_1.callExpression)((0, types_1.identifier)(observerName), [parentNode]);
        if (parentPath) {
            parentPath.replaceWith(observerCall);
            return true;
        }
        return false;
    }
    if ((0, types_1.isFunctionDeclaration)(funcNode)) {
        // Convert FunctionDeclaration to VariableDeclaration wrapped with observer
        const id = funcNode.id;
        const funcExpression = (0, types_1.functionExpression)(funcNode.id, funcNode.params, funcNode.body, funcNode.generator, funcNode.async);
        const observerCall = (0, types_1.callExpression)((0, types_1.identifier)(observerName), [
            funcExpression,
        ]);
        if ((0, types_1.isExportDefaultDeclaration)(functionPath.parent)) {
            functionPath.replaceWith(observerCall);
            return true;
        }
        if (id) {
            const _variableDeclarator = (0, types_1.variableDeclarator)(id, observerCall);
            const _variableDeclaration = (0, types_1.variableDeclaration)("const", [
                _variableDeclarator,
            ]);
            // Replace the function declaration with variable declaration
            functionPath.replaceWith(_variableDeclaration);
            return true;
        }
    }
    // For VariableDeclarators (e.g., const Func = () => {})
    if ((0, types_1.isVariableDeclarator)(funcNode) &&
        ((0, types_1.isFunctionExpression)(funcNode.init) ||
            (0, types_1.isArrowFunctionExpression)(funcNode.init))) {
        const init = funcNode.init;
        const observerCall = (0, types_1.callExpression)((0, types_1.identifier)(observerName), [init]);
        // @ts-ignore
        functionPath.get("init").replaceWith(observerCall);
        return true;
    }
    // For other cases where it's not already wrapped
    const observerCall = (0, types_1.callExpression)((0, types_1.identifier)(observerName), [
        // @ts-ignore
        funcNode,
    ]);
    functionPath.replaceWith(observerCall);
    return true;
}
function createPlugin(options) {
    return [
        exports.transform,
        {
            importPath: options.importPath,
            importName: options.importName || "observer",
            exclude: options.exclude || [],
        },
    ];
}
//# sourceMappingURL=index.js.map