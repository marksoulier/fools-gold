import { PluginObj } from "@babel/core";
export declare const transform: PluginObj;
export default function createPlugin(options: {
    importPath: string;
    importName?: string;
    exclude?: string[];
}): [typeof transform, Record<string, any>];
